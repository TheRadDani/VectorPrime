[pytest]
testpaths = tests
pythonpath = python
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    requires_fixtures: test needs downloaded model fixtures (tiny.gguf / tiny_onnx/)
    requires_gpu: test needs a physical GPU
